# Estrutura do Arquivo de Detalhes do Paciente

## Localização
- Arquivo: `/home/ubuntu/neuromodulation_mapper/app/(patient)/[id].tsx`

## Tabs Atuais
- info (📋)
- plan (📝)
- timeline (📅)
- effectiveness (📊)
- history (📜)
- audit (não visível no código visto)

## Tipo de Tab
```typescript
type Tab = "info" | "plan" | "history" | "audit" | "timeline" | "effectiveness";
```

## O que precisa ser feito
1. Adicionar nova tab "scales" (Escalas) com emoji 📈
2. Criar componente para exibir escalas do paciente
3. Integrar com `getPatientScaleResponses` da lib `scale-storage`
4. Mostrar histórico de escalas com scores e interpretações

## Estrutura de Dados de Escalas
- Arquivo: `lib/scale-storage.ts`
- Função: `getPatientScaleResponses(patientId, scaleType?)`
- Retorna: Array de ScaleResponse com id, patientId, scaleType, scaleName, date, totalScore, interpretation, notes

## Próximos Passos
1. Adicionar "scales" ao tipo Tab
2. Adicionar botão de tab para "Escalas"
3. Criar renderização condicional para exibir escalas
4. Criar componente ScalesList para listar escalas do paciente
